/**
 * Empty pause-space between chapters — the corridor between rooms.
 * Half a viewport on desktop, a third on mobile.
 */
export default function Corridor() {
  return <div aria-hidden className="h-[30vh] sm:h-[50vh]" />;
}
